# 最近の修正内容サマリー

このセッションで解決された主な問題と改善点の概要です。

______________________________________________________________________

## 1. TerraformとSQLによるテーブル作成の役割分担を明確化

- **問題:** `README.md`ではTerraformが全てのテーブルを作成するような誤解を招く記述になっていた。
- **解決策:** Terraformが作成するテーブルと、手動でのSQL実行が必要なテーブル（申請履歴、帳票用テーブル等）を`README.md`に明確に分けて記載した。

## 2. 履歴テーブルの命名と役割を整理

- **問題:** ドキュメント内に存在しないテーブル名 (`iam_policy_permissions_history`) が記載されており、「生の履歴」と「帳票用の整形済み履歴」の役割が曖昧だった。
- **解決策:** ドキュメントを修正し、`iam_policy_bindings_raw_history`（生の履歴）と`iam_permission_bindings_history`（帳票用）の役割を明確にした。

## 3. デプロイ・運用関連の改善

- **問題:** `bootstrap-deploy.sh`がSQLを誤った順序（アルファベット順）で実行し、デプロイに失敗する可能性があった。

- **解決策:** `bootstrap-deploy.sh`を修正し、テーブルとビューの依存関係を考慮した正しい順序でSQLを実行するようにした。

- **問題:** `terraform output`で確認できるCloud Schedulerジョブ名が一部不足していた。

- **解決策:** `terraform/outputs.tf`に不足していた3つのジョブ（不整合検知、期限切れ権限剥奪、履歴更新）の出力定義を追加した。

## 4. セキュリティと拡張性の向上 (Terraformリファクタリング)

- **問題:** IAMロールの定義が分散しており、将来の機能追加時に変更が煩雑になる可能性があった。
- **解決策:** `terraform`ディレクトリのコードを`bigquery`、`service_accounts`、`cloud_run`、`scheduler`、`monitoring`の各モジュールに分割。これにより、コードの再利用性が高まり、`main.tf`はモジュールの呼び出しに集約され、コードの可読性とメンテナンス性が大幅に向上した。

## 5. クリティカルなTerraform構文エラーの修正

- **問題**: Terraformの`for_each`と`count`が同時に指定されている箇所、および文字列内のダブルクォーテーションのエスケープ漏れにより、`terraform plan`や`apply`が実行時にクラッシュしていた。
- **解決策**:
  - `count`と`for_each`が同時に使われている`google_project_iam_member`および`google_organization_iam_member`リソースにおいて、`count`を削除し、`for_each`に条件式を適用することでエラーを解消した。
  - 監視アラートポリシーのログフィルター条件内のダブルクォーテーションを正しくエスケープし、構文エラーを修正した。

## 6. Pythonコードの品質向上 (Docstringの追加)

- **改善**: 全てのPythonファイルにGoogle Styleの日本語Docstringを追加。コードの可読性と保守性を向上させた。

## 7. ドキュメントの明確化

- **問題:** Webhook認証に必要なSecret Managerのシークレットについて、事前作成の必要性や設定方法が不明確だった。
- **解決策:** `README.md`に、`WEBHOOK_SECRET_NAME`等の具体的な変数名と、`saas.env`での設定方法を追記した。

## 8. クリティカルバグの修正

- **問題:** 期限切れ権限を剥奪するCloud Runジョブ (`/revoke_expired_permissions`) が、不変オブジェクトの属性を変更しようとしてクラッシュしていた。
- **解決策:** `cloud-run/app/main.py`を修正し、`dataclasses.replace()`を用いて新しいオブジェクトを生成することで、エラーを解消した。

## 9. セキュリティの改善 (Cloud Run)

- **問題 (セキュリティ):** Webhook認証のトークン比較に`==`演算子を使用しており、処理時間の差からシークレットを推測されるタイミング攻撃に対して脆弱だった。
- **解決策 (セキュリティ):** `secrets.compare_digest()`を使用するように変更し、処理時間を一定にすることで脆弱性を修正した。

## 10. 堅牢性の向上 (Cloud Run)

- **問題:** IAMポリシーの同時更新が発生した場合、APIが返す競合エラー（409 Conflict）をハンドリングできず、処理が失敗していた。
- **解決策:** `iam_executor.py`を修正し、競合エラー発生時に最新のポリシーを再取得して最大3回まで自動でリトライするロジックを実装。これにより、同時更新時にも処理が成功する可能性が高まった。

## 11. 開発・デプロイ環境の最適化

- **問題:** ローカル開発環境のPythonバージョンとCloud Run環境のPythonバージョンが異なっていた。また、`Dockerfile`がPoetryを使用しておらず、`requirements.txt`が存在しないためビルドできない状態だった。
- **解決策:**
  - `cloud-run/pyproject.toml`のPythonバージョン指定を`^3.12`に統一し、ローカル環境のPythonバージョンも3.12に切り替えた。
  - `cloud-run/Dockerfile`をPoetryを使ったマルチステージビルドに全面的に書き換え、本番用コンテナイメージのビルドプロセスを堅牢かつ効率的にした。

## 12. パフォーマンス改善 (Cloud Run)

- **問題:** 期限切れ権限剥奪ジョブで、ループ内で申請ごとにクエリが発行され、パフォーマンスが著しく低下する「N+1問題」が発生していた。
- **解決策:** `repository.py`を修正し、1回の`LEFT JOIN`クエリで必要な情報をまとめて取得するように変更。ループ内の都度のクエリ発行をなくし、パフォーマンスを大幅に改善した。また、この修正を検証するユニットテストも追加した。

## 13. 堅牢性の向上 (Google Apps Script)

- **問題:** Google Apps ScriptからCloud Runへの呼び出しが、一過性のエラーやCloud Runのコールドスタート時に失敗すると、リトライされずに処理が中断していた。
- **解決策:** `apps-script/Code.gs`の`callCloudRunExecute_`関数に、最大3回のリトライとエクスポネンシャル・バックオフ（1秒、2秒...と待機時間を増やす）のロジックを実装し、Cloud Runとの連携処理の堅牢性を向上させた。

## 14. セキュリティ強化: VPC-SCとCloud RunのIngress制限を構成可能に

- **問題:** Cloud Runのエンドポイントがインターネットに公開されており、第三者からのアクセスが可能だった。
- **解決策:** Terraformに変数を追加し、`enable_vpc_sc`フラグを`true`にすることで以下のセキュリティ設定が自動的に有効になるようにした。
  - **Cloud Run Ingress**: `INGRESS_TRAFFIC_INTERNAL_AND_CLOUD_LOAD_BALANCING`に変更され、内部トラフィックとロードバランサ経由に限定される。
  - **VPC Service Controls**: 関連するGoogle APIへのアクセスを制限するサービス境界（Perimeter）が構築される。
  - **APIの有効化**: VPC-SCに必要な`accesscontextmanager.googleapis.com` APIがフラグに連動して有効化される。
- **効果**: これにより、本番環境向けの高度なセキュリティ要件に、Terraformの変数を変更するだけで柔軟に対応できるようになった。

## 15. クリティカルバグの修正 (Cloud Run ジョブ)

- **問題:** Cloud Runの定期ジョブ (`/reconcile`, `/jobs/update-iam-bindings-history`) が、コンテナ内に存在しないSQLファイルを相対パスで読み込もうとして `FileNotFoundError` でクラッシュしていた。
- **解決策:** 外部のSQLファイルを読み込む設計を廃止し、SQLクエリを `repository.py` 内に直接埋め込むように変更。これにより、Dockerのビルドコンテキストに依存しない堅牢な設計となり、エラーを完全に解消した。また、この修正を検証するためのユニットテストも追加した。

## 16. 重大なセキュリティ脆弱性の修正 (認証バイパス)

- **問題:** Webhook認証において、環境変数 `WEBHOOK_SHARED_SECRET` が未設定の場合に認証がバイパスされ、誰でもエンドポイントを実行できてしまう「フェイルオープン」の脆弱性が存在した。
- **解決策:** `cloud-run/app/main.py` の `_authorize` 関数を修正し、シークレットが未設定の場合は常にアクセスを拒否する「フェイルクローズ」の設計に変更。これにより、設定ミスや環境変数の読み込みエラーが発生した場合でも、システムの安全性が保たれるようになった。

## 17. 設定の不整合を修正 (テーブル名の統一)

- **問題:** パイプラインジョブの結果を記録するテーブル名が、Terraform (`iam_pipeline_job_reports`) とアプリケーションコード (`pipeline_job_reports`) で異なっていた。これにより、Terraformで作成されたテーブルが使われず、アプリがエラーとなる不整合が発生していた。
- **解決策:** `cloud-run/app/repository.py` と `sql/001_tables.sql` のテーブル名をTerraformの定義に合わせて `iam_pipeline_job_reports` に統一し、インフラとアプリケーション間の設定の整合性を確保した。

## 18. 機能追加: 異常検知時のプロアクティブな通知 (アラート自動化)

- **問題:** バッチ処理の失敗や実行時エラーがBigQueryには記録されるものの、管理者が能動的に確認しに行かない限り異常を検知できない状態だった。
- **解決策:** GCPネイティブの仕組みを活用し、プロアクティブなアラート通知機能を実装した。
  - **Python:** アプリケーションのエラーハンドリング箇所で、標準の`logging`ライブラリを用いて`ERROR`レベルのログを出力するように修正。
  - **Terraform:**
    - `alert_notification_email` と `alert_notification_webhook_url` 変数を追加し、アラートの通知先（メール, Webhook）を設定可能にした。
    - Cloud Runサービスから`ERROR`レベル以上のログが検出された場合に発火する、ログベースのCloud Monitoringアラートポリシーを追加。
    - 設定された通知先（メール/Webhook）に応じて、Terraformが動的に通知チャネルを作成し、アラートポリシーに紐付ける構成とした。
- **効果:** これにより、アプリケーションでエラーが発生した際に、管理者が即座にメールやチャットで通知を受け取れるようになり、監視負荷の軽減と迅速な障害対応が可能になった。

## 19. 機能追加: 緊急アクセス（Break-glass）フローの実装

- **問題:** 障害対応時など、緊急でIAM権限が必要な場合に、通常の承認フローでは時間がかかりすぎるという課題があった。
- **解決策:** 緊急アクセス（Break-glass）のための自動承認フローを実装した。
  - **Googleフォーム:** 申請種別に「緊急付付与」を追加することを想定。
  - **Google Apps Script:** 「緊急」申請を検知すると、理由に`[緊急]`プレフィックスを付与し、システムによる自動承認として即座に`APPROVED`ステータスに変更。その後、Cloud Runの実行エンドポイントを直接呼び出す。
  - **Cloud Run:** 実行時に理由に`[緊急]`が含まれている場合、`[BREAK-GLASS]`というマーカー付きの警告ログを出力する。
  - **Terraform:** `[BREAK-GLASS]`ログを検知する新しいCloud Monitoringアラートポリシーを追加。既存の通知チャネルを利用して、緊急アクセスが実行されたことを管理者に即時通知する。
- **効果:** 監査証跡を完全に残しつつ、緊急時の権限付与を迅速化。同時に、強いアラートによって不正利用を抑止し、統制とスピードを両立させた。

## 20. テストの強化: 重要シナリオ（ユースケース）テストの実装

- **課題:** これまでのテストは、個別の関数が正しく動作するかを確認する単体テストが中心であり、複数のコンポーネントが連携するユースケース全体を通して、ビジネス要件が担保されているかを保証するテストが不足していた。
- **解決策:** システムの中核機能である `/execute` エンドポイントについて、業務上絶対に壊れてはならない5つの重要シナリオを定義し、振る舞い駆動のシナリオテスト (`test_main_execute_flow.py`) として実装した。
  1. **正常系:** 承認済みの申請が正しく実行され、成功ログが記録されること。
  1. **冪等性:** 成功済みの申請を再実行しようとしても、APIが呼ばれずスキップされること。
  1. **状態遷移の防御:** 「承認済」以外のステータスの申請は、実行されないこと。
  1. **管理スコープの防御:** 管理対象外のリソースへの申請は、エラーとして拒否されること。
  1. **フェイルセーフな監査:** API実行時に例外が発生しても、必ず「失敗」のログが記録されること。
- **効果:** 機能追加やリファクタリングの際に、意図せずコアな業務要件を破壊してしまう「デグレード」を自動的に検知できるようになった。これにより、システムの信頼性と保守性が大幅に向上した。

## 21. データパイプラインとマスタ同期の未実装バグ・不要コードの解消

- **問題:** 設計・ドキュメント上は存在するが、プログラムの実態として以下の問題が存在した。
  1. `iam_policy_bindings_raw_history` (監査用の生履歴) にデータを日次で追記するコレクターが未実装だった。
  1. `principal_catalog` (プリンシパルマスタ) を日々最新化する定期更新バッチが未実装だった。
  1. Cloud Run (Python) からステータスをUPDATEする際、`updated_at` (更新日時) が現在時刻に更新されないバグがあった。
  1. `iam_permission_matrix` という過去の設計の残骸（SQLファイルおよびドキュメント記述）が放置されていた。
- **解決策:**
  - 不要な `iam_permission_matrix` 関連のSQLスクリプトとドキュメント記述を削除し、リポジトリをクリーンアップした。
  - `repository.py` および `main.py` を修正し、既存の `/jobs/update-iam-bindings-history` (日次バッチ) の処理内に、生のIAM履歴の追記と、プリンシパルマスタの `MERGE` (同期) 処理を組み込んだ。
  - `repository.py` の `update_request_status` 実行時に、明示的に `updated_at = CURRENT_TIMESTAMP()` をセットするよう修正した。
- **効果:** コードとドキュメントの乖離が完全に解消され、システムがデータの鮮度を自律的に維持できるようになった。

## 22. Terraform初回デプロイ時のシークレット関連クラッシュの修正

- **問題:** 新しいGCP環境でTerraformを初めて実行する際、`var.webhook_secret_name`で指定されたSecret Managerのシークレットが手動で事前に作成されていない場合、TerraformがそのシークレットにIAM権限を付与しようとしてクラッシュしていた。
- **解決策:** Terraformの設定（`terraform/main.tf`）を修正し、`var.webhook_secret_name`で指定されたシークレットを`google_secret_manager_secret`リソースとしてTerraform自身が作成するように変更した。また、初期ダミー値を持つシークレットバージョンも同時に作成し、その後の手動変更を`ignore_changes`で無視するように設定した。これにより、シークレットが存在しない状態でのIAM権限付与エラーを回避し、初回デプロイが成功するようにした。
- **効果:** まっさらなGCP環境への初回デプロイの安定性が向上し、運用者が事前にシークレットを手動で作成する必要がなくなった。

## 23. Terraformコードのセキュリティ脆弱性チェック (tfsec) の導入

- **問題:** Terraformコード自体に潜在するセキュリティ上の問題（公開範囲の広すぎる設定や、暗号化の未有効化など）を自動で検知する仕組みがなく、レビュー担当者の目視に依存していた。
- **解決策:** GitHub ActionsのCIパイプライン（`.github/workflows/ci.yml`）に、`tfsec` (aquasecurity/tfsec-action) による静的解析ステップを追加した。Pull Request作成時・更新時に自動でセキュリティ・ベストプラクティスへの準拠状況がスキャンされる。（※既存環境への影響を考慮し、初期導入としてマージをブロックしない `soft_fail: true` を設定）
- **効果:** レビュアーが気づきにくいインフラのセキュリティリスクをツールが自動で指摘・検知できるようになり、より安全でモダンなDevSecOpsの運用基盤が整った。

## 24. 安全な Terraform Destroy 手順の明文化

- **問題:** 環境を破棄・再構築する際、APIの無効化による他システムへの影響や、監査ログの消失リスクに関する明確な手順書が不足していた。
- **解決策:** `README.md` および `terraform/README.md` に、`terraform destroy` を実行した際のフェイルセーフな挙動（APIは無効化されない、BigQueryテーブルは保護される）と、安全な削除手順をドキュメント化した。
- **効果:** 運用者が安心して環境のクリーンアップを行えるようになり、オペレーションミスによる大事故（API無効化による二次被害など）を防ぐことができるようになった。

## 25. 開発者向けドキュメントサイト (MkDocs) の導入

- **問題:** Pythonコード内のGoogle Style Docstringsや、マークダウン形式の運用マニュアル群がリポジトリ内に散在しており、開発者や運用者がシステム仕様を横断的に閲覧しにくい状態だった。
- **解決策:** `mkdocs-material` と `mkdocstrings` を導入し、静的サイトジェネレーターによる統合ドキュメントサイトを構築した。`mkdocs.yml` を設定し、`docs/api/` 配下にPythonモジュールのAPIリファレンスを自動生成する仕組みを整えた。
- **効果:** クラス定義、メソッド、型ヒントなどが美しいHTMLとして可視化されるようになり、新規参画エンジニアのオンボーディングコスト低下と、コード仕様の理解スピードが劇的に向上した。

## 26. 設計の残骸（Dead Code）の削除と設定不整合の解消

- **問題:** 度重なるリファクタリングの結果、使われなくなったSQLファイル（`003_reconciliation.sql`, `008_update_bindings_history.sql`）が残り、ドキュメントの記述と実際の挙動が乖離していた。また、Webhook認証のフェイルクローズ化に伴い、インフラ（Terraform）とアプリ（GAS）間でシークレット初期値にすれ違いが生じ、初回デプロイ時に必ず認証エラー（401 Unauthorized）になる致命的な矛盾があった。さらに、TerraformとSQLでテーブル定義（DDL）が二重管理されていた。
- **解決策:** 不要なSQLファイルを完全に削除し、各種READMEの記述を実態に合わせて修正した。`saas.env.example`にWebhookシークレットのTerraform初期ダミー値を明記し、GAS側の設定を「任意」から「必須」へ変更。さらに、`001_tables.sql`からTerraform管理下のテーブルDDLを削除し、管理元を一本化した。
- **効果:** Single Source of Truth（信頼できる唯一の情報源）が徹底され、新規参画者が古いファイルやドキュメントに惑わされることがなくなった。また、新規デプロイ時にWebhook連携が確実に成功し、セキュアなフェイルクローズ設計が正しく機能する完璧な状態となった。

## 27. アーキテクチャの完全独立化（外部システム依存の排除）

- **問題:** 従来は、最新のIAMポリシー状態（`iam_policy_permissions`）の収集を外部システムに依存しており、システム単体でデータが完結しないアーキテクチャ上の課題があった。また、外部システムはPythonプロセス内でグループメンバーを展開する重いETL処理を行っており、タイムアウトやクォータ制限のリスクを抱えていた。
- **解決策:** Cloud Asset APIを用いてIAMポリシーを高速に収集し、BigQueryのテーブルを洗い替える専用コレクター（`/collect/iam-policies`）を本システム内に新設した。グループ展開の重い処理は行わず、生のバインディング情報をそのままロードするモダンなELTアプローチを採用。これに伴いTerraformのスケジューラ、CI/CD変数の配線、およびユースケーステストを追加した。
- **効果:** 外部システムへの依存が完全に排除され、本リポジトリ単体で完結する堅牢なSaaS基盤へと進化した。

## 28. セキュリティ強化: GASからのOIDC認証連携（Webhookシークレット廃止）

- **問題:** GASからCloud Runへの連携において、共通鍵（Webhook Shared Secret）を用いた認証を行っていたが、シークレット管理の負担や、将来のVPC-SC（VPC Service Controls）導入時のアーキテクチャ上の障壁となる課題があった。
- **解決策:** GAS側でGCPの `IAM Credentials API` を呼び出し、Cloud RunをAudienceとする正しいOIDCトークンを動的に生成（Impersonation / 権限借用）して認証する方式（方針B）に変更した。Terraform側でGAS用のサービスアカウントを作成し、Cloud Runの `run.invoker` ロールを付与。また、Python側の独自認証ロジックを整理し、標準のOIDCトークン検証に統合した。
- **効果:** 共通鍵の管理という負債が完全に解消され、セキュアでモダンなクラウドネイティブ認証が確立された。また、将来的にVPC-SCを有効化するエンタープライズ構成（方針A）へ移行する際の基盤が整った。

## 29. 機能追加: GCP直接変更（野良権限）のインシデント検知と可視化

- **問題:** 運用ルールを外れてGCPコンソール等から直接付与されたIAM権限（シャドーITや緊急の手動対応）をシステムが検知できず、監査上のリスクや不要な権限が残留する課題があった。
- **解決策:** 不整合検知（リコンシリエーション）処理を拡張し、インシデント管理モデルを実装した。
  - **完全外部結合 (FULL OUTER JOIN) による差分検知:** `repository.py` のSQLを拡張し、システム内に承認済み申請がないのにGCP側に存在する権限を `UNMANAGED_BINDING`（未管理バインディング）として検出し、深刻度 `HIGH` で `iam_reconciliation_issues` テーブルに記録するようにした。
  - **プロアクティブな監視と通知:** 不整合が検知されると、Cloud Runが `[RECONCILIATION_ISSUE_DETECTED]` ログを出力。Terraformで構成した Monitoring Alert Policy がこれを検知し、管理者のSlackやメールへ即時通知を飛ばす仕組みを追加した。
  - **GASによるインシデントの可視化:** スプレッドシートのカスタムメニューに「不整合アラート（インシデント）の確認」を追加。`OPEN` 状態のインシデントが専用シートに赤色ベースのヘッダーとともに一覧表示され、未管理権限や期限切れ権限をシステムにログインすることなく即座に把握できるようにした。
- **効果:** システム外での手動変更を正規の監査証跡に同化させることなく「インシデント」として厳格に分離・可視化し、セキュアな統制と迅速な異常検知プロセスを確立した。

## 30. アーキテクチャ改善: GASからのBigQuery直接DMLの廃止とAPI集約

- **問題:** GASがBigQueryに対して直接 `INSERT` や `UPDATE` のDMLを発行しており、コスト増や遅延、および同時実行制限のリスクを抱えていた。
- **解決策:** Cloud Run上にリクエスト登録・更新用の専用バックエンドAPIエンドポイントを新設し、GAS側の処理をHTTPリクエストに集約。また、Python側での `INSERT` はBigQueryのストリーミング挿入（`insert_rows_json`）に切り替えた。
- **効果:** DMLの頻発を防ぎ、パフォーマンスを向上させるとともに、データベースアクセスをバックエンドに集約してよりセキュアで管理しやすいSaaSアーキテクチャを実現した。

## 31. アーキテクチャ改善: ステータス文字列の集約とAPI化

- **問題:** アプリケーション内で利用するステータス文字列（APPROVED, PENDING等）がGAS、Python、BigQueryマスタで分散定義されており、変更時の同期漏れリスクがあった。
- **解決策:**
  - Cloud RunにステータスマスタをBigQueryから取得・提供する専用API（`/api/statuses`）を新設した。
  - GASのステータス正規化処理を、ハードコードからAPI動的取得（＋1時間のインメモリキャッシュ）へ移行した。
  - Pythonバックエンドに定数管理用モジュール（`constants.py`）を新設し、分散していた文字列を一元管理する土台を整えた。
  - SQLのステータスマスタ（`004_workbook_tables.sql`）を、システムで実際に利用される英語コード（APPROVED等）と完全に一致するよう修正した。
- **効果:** Single Source of Truth（ステータス定義の単一障害点排除）が実現し、新しいステータスを追加する際の保守性と拡張性が飛躍的に向上した。

## 32. ドキュメント改善: テストのモックアーキテクチャとCI環境の明記

- **問題:** `DEVELOPING.md` において、テストコードがGCP環境に依存せず完全にモック化されていることや、GitHub ActionsによるCIパイプライン（WIF連携）が構築済みであることが明確に記載されておらず、レビューアに「CI環境でのSA管理が必要ではないか」という懸念を抱かせていた。
- **解決策:** `DEVELOPING.md` のテストセクションを更新し、テストがGCPクレデンシャル不要のオフライン状態で実行可能であること、およびCIパイプラインの存在を明記した。
- **効果:** 新規開発者やセキュリティ監査担当者が、安全でセキュアなテスト・デプロイ環境がすでに構築されていることを正しく理解できるようになった。

## 33. 機能改善: 堅牢なGemini AI承認支援（自動推論）の実装

- **問題:** 申請者向けのGemini提案アシスタントがWebアプリとして独立しており、使用が任意であるため、依然として過大権限が申請されるリスクがあり、承認側での妥当性判断が困難だった。
- **解決策:** 申請が行われた直後（`onFormSubmit`）に、システムがバックグラウンドでGemini APIへ「やりたいこと」を送信し、推論された「最小権限の推奨ロール」をスプレッドシート（承認画面）の専用列に自動追記するようアーキテクチャを変更した。
- **効果:** レビュアーは、申請内容とAIの推奨ロールを横並びで比較しながら承認判断を行えるようになり、過剰な権限付与を防ぐ運用が強制的に担保（堅牢化）された。

## 34. ドキュメント改善: 運用手順書(Runbook)を最新アーキテクチャに同期

- **問題:** `docs/operations-runbook.md` に、すでに廃止されたWebhookシークレット管理の手順や、削除済みのSQLファイルを実行する手順など、古いアーキテクチャの記述が残留しており、運用時の混乱を招く状態だった。
- **解決策:**
  - Secret Manager に関連する不要なIAMロール要件および付与コマンドを削除した。
  - SQLの実行手順を最新の5ファイル（`003`, `008` 削除済み）の構成に更新した。
  - Cloud Scheduler の日次ジョブ確認コマンドに、不整合検知や履歴更新などの新ジョブを追加した。
  - GASからCloud Runへの通信に関する記述を「Webhook」から「バックエンドAPI通信」へ修正した。
- **効果:** 運用手順書が現在の最新アーキテクチャ（OIDC認証・API集約・インシデント管理）と完全に一致し、SREやインフラ担当者が迷わず安全にシステムを保守できるようになった。

## 35. バグ修正: GASの構文エラー解消および緊急アクセス時のUI不整合修正

- **問題:** `Code.gs` に不要な閉じカッコが残留しておりデプロイ不能（SyntaxError）になるバグと、緊急アクセス（Break-glass）発動時にBigQuery側は承認済になる一方で、スプレッドシートのステータスが「申請中」のままになる不整合が存在した。
- **解決策:** 構文エラーを修正し、`Code.gs` 内にスプレッドシートのステータスセルを直接書き換える `updateSheetStatus_` 関数を追加。緊急アクセスの処理フロー内でこれを呼び出すようにした。
- **効果:** システムのデプロイが正常に行えるようになり、緊急対応時にも監査証跡（BigQuery）と運用UI（スプレッドシート）のステータスが完全に同期するようになった。

## 36. バグ修正: 承認時のIAM実行API（`/execute`）呼び出しエラーの解消とコード共通化

- **問題:** 運用手順書の指示通りにルートURLを設定した場合、GASの `callCloudRunExecute_` 関数が `/execute` パスを付与せずにリクエストを送信してしまい、404エラーでIAM権限の自動付与が確実に失敗する致命的なバグがあった。
- **解決策:** `callCloudRunExecute_` 関数内に残っていた古い独自のリトライ・フェッチ処理を削除し、先日新設した安全なAPI呼び出し共通関数 `callCloudRunApi_` を利用するよう1行にリファクタリングした。
- **効果:** URLパスの組み立てが自動で正しく行われるようになり、承認時のIAM権限付与が確実に成功するようになった。同時に、GAS側の冗長なコードが削減され保守性が向上した。

## 37. バグ修正: 「期限付き権限の自動剥奪」機能の入力欠落とステータスエラーの解消

- **問題:** Googleフォームの「利用期限」項目がGAS側で読み取られておらず、BigQueryに常に `NULL` が登録されるため、期限切れ権限の自動剥奪ジョブが事実上のデッドコード（1件も対象を拾えない状態）になっていた。また、自動剥奪対象の権限が手動で既に削除されていた場合、成功(SKIPPED)ではなく失敗として誤記録されるバグがあった。
- **解決策:** GASの `Code.gs` を修正し、フォームの「利用期限」フィールドから日付をパースして `expires_at` としてBigQueryへ連携するよう実装。また、スプレッドシート（承認画面）にも「expires_at」列を追加し、承認者が期限を視認できるようにした。バックエンドのPython実装も修正し、既に剥奪済みだった場合は `REVOKED_ALREADY_GONE` ステータスを正しくセットするよう改善した。
- **効果:** 死んでいた自動権限剥奪機能が完全に息を吹き返し、期間限定アクセスという強力なセキュリティ統制が仕様通りに機能するようになった。

## 38. バグ修正: SQLクエリの直積（Cartesian Product）によるデータ重複と誤検知の解消

- **問題:** 同一のプリンシパル・リソース・ロールに対して複数回の申請履歴（例: 付与→剥奪→再付与）が存在する場合、`repository.py`内の `FULL OUTER JOIN` や `LEFT JOIN` がすべての履歴レコードと結合してしまい、棚卸し帳票のデータが重複（爆発）するバグ、および古い履歴に引きずられて不整合検知が誤検知（False Positive）を起こすバグが潜んでいた。
- **解決策:** `repository.py` の不整合検知ジョブ（`/reconcile`）および履歴更新ジョブ（`/jobs/update-iam-bindings-history`）のSQLを修正し、分析関数 (`ROW_NUMBER() OVER ...`) および `QUALIFY` 句を用いて「該当バインディングに対する最新の申請レコード」のみを結合対象とするようリファクタリングした。
- **効果:** 運用が長期化して申請データが蓄積されても、棚卸し帳票のレコード数や不整合検知の結果が常に正確に保たれるようになった。

## 39. バグ修正: 運用開始後に発火する「時限爆弾」的な致命的バグの解消

- **問題:**
  - **BigQuery仕様の罠:** 申請をStreaming Insertで記録していたため、緊急アクセス（Break-glass）など直後にUPDATEが走る処理において行がバッファから見つからず、ステータス更新が空振り（PENDINGのままスタック）して権限付与が確実に失敗する致命的バグがあった。
  - **ジョブスケジュールの矛盾:** 期限切れ権限の剥奪（5:00 AM）が、不整合検知（4:00 AM）よりも後に設定されていたため、権限が切れる当日に毎日必ず「期限切れなのに権限が残っている」という重大インシデントアラートが全件誤報される運用崩壊バグがあった。
  - **UI表示のバグ:** スプレッドシート帳票上でステータスが英語コード（APPROVED等）のまま表示されてしまう箇所が存在した。
- **解決策:**
  - `repository.py` の `insert_access_request_raw` を直接のDML（`INSERT INTO`）へ変更し、直後のUPDATEが確実にヒットするように修正した。
  - `terraform/variables.tf` の剥奪ジョブを `0 1 * * *`（1:00 AM）に前倒しし、現状収集の前に剥奪が完了するようスケジュールを最適化した。
  - `005_workbook_views.sql` にステータスマスタを結合し、必ず日本語でステータスが表示されるよう修正した。
- **効果:** エンタープライズ運用に耐えうる、真に信頼性の高い「自動化とアラートのサイクル」が完成した。

## 40. バグ修正: 権限剥奪（REVOKE）時のリコンシリエーション誤検知バグの解消

- **問題:** 不整合検知（`/reconcile`）のSQLが申請種別（`request_type`）を考慮していなかったため、権限の「削除（REVOKE）」が正常に完了しGCPから権限が消えると、「承認済なのに権限が付与されていない（APPROVED_NOT_APPLIED）」というHIGHレベルのアラートが毎回誤発報（False Positive）される致命的な論理バグがあった。また、期限切れ自動剥奪ジョブが「削除申請」自体を再度剥奪しようとする潜在バグがあった。
- **解決策:** `repository.py` のSQLを改修し、申請種別が `REVOKE` の場合とそれ以外（`GRANT/CHANGE`）で不整合の判定ロジック（`issue_type` と `severity`）を分岐させた。また、期限切れ判定SQLに `request_type != 'REVOKE'` の条件を追加した。
- **効果:** 権限の剥奪フローにおいても不整合検知が正確に機能するようになり、運用者を疲弊させる誤報アラート（狼少年化）を未然に防ぐことができた。

## 41. 重大バグ修正: IAM Condition(条件付き権限)の消失防止とビューのデータ爆発解消

- **問題:** - **セキュリティ/データ喪失:** `iam_executor.py` がGCPからIAMポリシーを取得する際、APIの仕様によりデフォルトのVersion 1をリクエストしていた。これにより、対象プロジェクトに設定されていたタグベース等の条件付きバインディング（IAM Condition）がレスポンスから欠落し、そのままUPDATEされることで「既存の条件付き権限がすべて消去される」という不可逆で致命的なバグがあった。
  - **データ爆発:** 帳票表示用のBigQueryビュー（`005_workbook_views.sql`）において、履歴テーブルを最新のスナップショットで絞り込む条件（`WHERE execution_id = ...`）が抜け落ちていた。これにより、削除されたリソースがゴースト化して残り続けたり、「権限設定マトリクス」の行数が運用日数に応じて指数関数的に増殖し、スプレッドシートをクラッシュさせるバグがあった。
- **解決策:**
  - `iam_executor.py` の `getIamPolicy` リクエストに `{"options": {"requestedPolicyVersion": 3}}` を明示的に付与し、IAM Conditionを含む完全なポリシーを保持するように修正した。
  - `005_workbook_views.sql` の全ビューをリファクタリングし、`LatestSnapshot` CTE等を用いて必ず「最新の1回分のバッチ実行データ」のみを帳票に展開するように安全化・高速化した。
- **効果:** エンタープライズ環境の既存の複雑なIAM設定（JITアクセス等）を破壊するリスクがゼロになり、運用が数年続いても帳票（スプレッドシート）が軽く、正確で、壊れない本番稼働レベルのELTパイプラインが完成した。

## 42. 重大バグ修正: CI/CDパイプラインによる設定破壊と監査データ欠落の解消

- **問題:** - **CI/CDの時限爆弾:** `.github/workflows/ci.yml` が生成するTerraform変数ファイルから `gas_trigger_owner_email` やアラート通知先の設定が漏れていたため、次回のPRマージ時にTerraformが「設定削除」と判定し、GASからのOIDC認証権限を強制剥奪してシステム全体を停止させる致命的なバグが潜んでいた。
  - **監査データの欠落:** `repository.py` が生のIAM履歴（Raw History）を保存する際、`scope` と `resource_type` カラムに `NULL` をハードコードして挿入しており、重要な監査情報が永久に失われるバグがあった。
- **解決策:**
  - `ci.yml` を修正し、不要な古いシークレット変数を削除した上で、GitHub Variables/Secrets から GASオーナーアドレスや通知先URLを正しく引き継ぐようパイプラインを改修した。
  - `repository.py` のSQLを修正し、現在のIAM状態から `scope` と `resource_type` を正しく抽出して履歴テーブルにインサートするようにした。
- **効果:** 次回のデプロイ（マージ）時におけるシステム停止事故を未然に防ぎ、外部監査にも耐えうる欠落のない完全な監査ログが蓄積されるようになった。

## 43. 重大バグ修正: 自動剥奪ジョブのN+1タイムアウト回避と監査イベントログのデータロスト解消

- **問題:** - **DMLのN+1エラー (時限爆弾):** 期限切れ自動剥奪ジョブが権限のステータスを更新する際、ループ内で1件ずつBigQueryのDML (UPDATE) を発行していたため、剥奪対象が同日に複数件存在すると同時実行による競合エラー（Concurrent DML）やCloud Runのタイムアウト上限（15分）を引き起こし、処理が永遠に完了しなくなる重大なアーキテクチャ欠陥があった。
  - **データロスト:** Python側でステータスを自動更新（`APPROVED` → `REVOKED`等）した際、GAS側とは異なり「なぜ変わったのか」を示す監査イベントログ（`iam_access_request_history`）への追記が完全に抜け落ちており、コンプライアンス上の証跡が消失するバグがあった。
- **解決策:** `repository.py` に、複数のリクエストのステータスをCASE文を用いて1回のクエリで一括UPDATEし、同時に監査イベントログをStreaming Insertで一括追記する `bulk_update_request_status_and_history` メソッドを新設。`main.py` の自動剥奪ジョブをこれを利用する形にリファクタリングした。
- **効果:** BigQuery特有の仕様であるDML制限を完全に回避し、期限切れ権限が1日に何百件発生してもタイムアウトせずに超高速で安全に処理できるようになった。同時に、システムによる自動剥奪の記録が監査ログに欠落なく残るようになった。

## 44. 重大バグ修正: 本番稼働後のUX崩壊およびCI/CDクラッシュの防止

- **問題:**
  - **CI/CDクラッシュ:** `.github/workflows/ci.yml` 内で `tfvars` を生成する際、クォーテーションのエスケープが漏れていたため、無効なHCL構文が生成され、次回のデプロイ時にTerraformが100%クラッシュする状態になっていた。また、ジョブ時間の修正（1:00 AM前倒し）もCI側に反映されていなかった。
  - **早朝強制剥奪のバグ:** GASが「利用期限」を深夜0:00として保存していたため、深夜1:00の自動剥奪ジョブにより「利用期限の当日の早朝に権限が剥奪され、その日丸一日利用できなくなる」という重大な運用障害（UX崩壊）が発生する仕様になっていた。
  - **データ重複のバグ:** コアビューである `002_views.sql` において、`QUALIFY` 句による最新化が行われておらず、過去の申請履歴と直積結合（Cartesian Product）を起こしてデータが爆発するバグが残存していた。
- **解決策:**
  - `ci.yml` を修正し、`tfvars` への出力時に変数を `\"` で囲むようエスケープを徹底。同時にスケジュールを `0 1 * * *` へ修正した。
  - `Code.gs` を修正し、指定された利用期限日の `23:59:59.999` を有効期限として設定し、当日の早朝剥奪を防止した。
  - `002_views.sql` の結合部分を `QUALIFY ROW_NUMBER()` を用いた最新1件の絞り込みに変更し、データ重複を根絶した。
- **効果:** CI/CDによる自動デプロイが安全に成功するようになり、利用者のアクセス体験が損なわれる障害や、長期運用によるデータベース崩壊のリスクが完全に排除された。

## 45. 重大バグ修正: 本番運用で即発火する「UX崩壊・認証エラー・条件付き権限の巻き込み」の防止

- **問題:**
  - **条件付き権限への誤爆と剥奪漏れ:** `iam_executor.py` が、条件（IAM Condition）を持つ既存のロールバインディングに対して無差別に新規ユーザーを追加・剥奪してしまうため、意図しない条件付き権限の付与や、複数の条件を持つユーザーの権限剥奪漏れ（セキュリティインシデント）が発生するバグがあった。
  - **一括承認時のコピペ全損バグ:** GASの `onEdit` 関数が「左上の1セル」しか処理しないため、管理者が複数行を選択して「承認済」をコピペ等で一括反映した場合、最初の1件以外がすべて無視され、権限が付与されないまま画面だけ承認済になる運用障害の罠があった。
  - **OIDC Audience 不一致による常時401エラー:** Cloud Run内部での `request.url_root` が `http://` となるため、GASが発行した `https://` のトークンとAudience不一致を起こし、API実行が100%失敗する状態だった。
- **解決策:**
  - `iam_executor.py` の `_apply_diff` をリファクタリング。GRANT時は「条件なし」のバインディングを明示的に探して付与し、REVOKE時は対象ロールの「すべてのバインディング」から確実にメンバーを剥奪するよう修正した。
  - `Code.gs` の `onEdit` 関数をループ処理に書き換え、範囲編集（ドラッグやコピペによる複数行の同時変更）されたすべてのリクエストを漏れなくBigQueryとCloud Runへ連携するようにした。
  - `main.py` の認証ロジックで、内部解決されたURLを強制的に `https://` へ置換してからOIDCトークンのAudienceを検証するように修正した。
- **効果:** エンタープライズ特有の複雑なIAM環境（JITアクセス等）を破壊するリスクが根絶され、運用担当者の実際の操作（一括承認など）にも完璧に追従する、真の本番稼働レベルのシステムが完成した。

## 45. 重大バグ修正: 本番運用で即発火する「UX崩壊・認証エラー・条件付き権限の巻き込み」の防止

- **問題:**
  - **条件付き権限への誤爆と剥奪漏れ:** `iam_executor.py` が、条件（IAM Condition）を持つ既存のロールバインディングに対して無差別に新規ユーザーを追加・剥奪してしまうため、意図しない条件付き権限の付与や、複数の条件を持つユーザーの権限剥奪漏れ（セキュリティインシデント）が発生するバグがあった。
  - **一括承認時のコピペ全損バグ:** GASの `onEdit` 関数が「左上の1セル」しか処理しないため、管理者が複数行を選択して「承認済」をコピペ等で一括反映した場合、最初の1件以外がすべて無視され、権限が付与されないまま画面だけ承認済になる運用障害の罠があった。
  - **OIDC Audience 不一致による常時401エラー:** Cloud Run内部での `request.url_root` が `http://` となるため、GASが発行した `https://` のトークンとAudience不一致を起こし、API実行が100%失敗する状態だった。
- **解決策:**
  - `iam_executor.py` の `_apply_diff` をリファクタリング。GRANT時は「条件なし」のバインディングを明示的に探して付与し、REVOKE時は対象ロールの「すべてのバインディング」から確実にメンバーを剥奪するよう修正した。
  - `Code.gs` の `onEdit` 関数をループ処理に書き換え、範囲編集（ドラッグやコピペによる複数行の同時変更）されたすべてのリクエストを漏れなくBigQueryとCloud Runへ連携するようにした。
  - `main.py` の認証ロジックで、内部解決されたURLを強制的に `https://` へ置換してからOIDCトークンのAudienceを検証するように修正した。
- **効果:** エンタープライズ特有の複雑なIAM環境（JITアクセス等）を破壊するリスクが根絶され、運用担当者の実際の操作（一括承認など）にも完璧に追従する、真の本番稼働レベルのシステムが完成した。

## 46. 重大バグ修正: GASからCloud Runへの連携におけるAudience不一致とタイポの修正

- **問題:** - **常時401認証エラー:** GASの `getOidcToken_` 関数において、Cloud Runの `/execute` パスを含んだままOIDCのAudienceとして使用していたため、Audience不一致が発生しすべてのAPI呼び出しが失敗する致命的なバグがあった。
  - **Pythonのタイポ:** `main.py` の `_authorize` メソッド内で `replace("http://", "https://")` が2重に記述されていた。
  - **GASエラーハンドリング:** `onEdit` トリガー内で `request_id` 列が見つからなかった場合のガードレールが甘かった。
- **解決策:**
  - `Code.gs` を修正し、Audience生成時に `props.cloudRunUrl` から `/execute` パスを正規表現で削除するようにした。
  - `main.py` の不要な重複置換コードを削除した。
  - `Code.gs` の `onEdit` 関数内に `reqIdCol < 0` の早期リターンを追加した。
- **効果:** GASからCloud Runへの認証が正しく通り、一連の自動実行フローが安全かつ確実に動作するようになった。

## 47. 重大バグ修正: BigQueryストリーミングバッファとDML競合によるグループ収集ジョブのクラッシュ解消

- **問題:** `repository.py` の `replace_groups` において、特定のソースのデータを `DELETE` した直後に `insert_rows_json` (Streaming API) を用いてデータを挿入していた。これにより、BigQueryのストリーミングバッファにデータが残っている間（最大90分）に次回のジョブが走ると、`DELETE` がバッファと競合して確定でクラッシュする時限爆弾バグが存在した。
- **解決策:** Streaming API の使用をやめ、挿入処理を `bigquery.LoadJobConfig(write_disposition="WRITE_APPEND")` を用いた一括ロード（Load Job）に変更した。Load Jobはストリーミングバッファを使用しないため、次回実行時の `DELETE` と競合しない。
- **効果:** 他ソースのデータを安全に保護しつつ、グループ収集ジョブが短期間に何度走ってもBigQueryのDML制約によるエラーが発生しなくなった。

## 48. 重大バグ修正: 履歴更新ジョブにおけるBigQueryの型不一致エラーの解消

- **問題:** `repository.py` の `run_update_bindings_history_job` において、`TIMESTAMP` 型の `expires_at` を `DATE` 型の `next_review_at` カラムに暗黙的に挿入しようとしていた。BigQueryは `TIMESTAMP` から `DATE` への暗黙キャストをサポートしていないため、日次の履歴更新バッチが100%クラッシュする時限爆弾バグが存在した。
- **解決策:** SQLクエリ内で `CAST(req.expires_at AS DATE) AS next_review_at` と明示的にキャストを行うように修正した。
- **効果:** 日次の帳票用データ生成バッチが型エラーで落ちることなく、安定して稼働するようになった。

## 49. アーキテクチャ改善: スキーマの正規化、型エラー修正、およびインシデント重複の防止

- **問題:**
  - `iam_permission_bindings_history` などの下流テーブルに、`resource_name` と全く同じ値を持つ冗長な `resource_full_path` カラムが存在しており、二重管理の技術的負債および存在しないカラムの参照エラーを生んでいた。
  - 履歴更新ジョブで `TIMESTAMP` を `DATE` 型へ暗黙キャストしようとしてクラッシュするバグがあった。
  - 不整合検知ジョブが、既にOPENなインシデントを考慮せずINSERTするため、同じアラートが毎日無限増殖（狼少年化）する課題があった。
- **解決策:**
  - 冗長な `resource_full_path` カラムをDDL、ビュー、およびPythonのSQLクエリから完全に削除（パージ）し、実態に即して `resource_name` に統一（正規化）した。
  - 履歴ジョブSQL内で `CAST(req.expires_at AS DATE)` と明示的な型変換を追加した。
  - 不整合検知ジョブのSQLに `NOT EXISTS` 条件を追加し、同じプリンシパル・リソース・ロールのインシデントが既に存在する場合は新規登録をスキップするよう修正した。
- **効果:** データモデルが極めてクリーンになり、バッチ処理のクラッシュバグが根本から消滅した。また、運用者を疲弊させるアラートのノイズが根絶された。

## 50. ドキュメント修正: BigQueryテーブル仕様書の実態との同期

- **問題:** `docs/bigquery_tables.md` に記載されている `iam_permission_bindings_history` および `iam_policy_permissions` のテーブル定義が、実際のTerraform定義およびSQLの実態と乖離しており（存在しないカラムが記載されている等）、開発者を混乱させる負債となっていた。
- **解決策:** ドキュメントから削除済みの `resource_full_path` 等の記述をパージし、Terraformの `main.tf` で定義されている実際のスキーマ（`execution_id` や `assessment_timestamp` など）と完全に一致するようにテーブル仕様書を書き直した。
- **効果:** ドキュメントとコードの乖離（嘘のドキュメント）がなくなり、Single Source of Truthが保たれるようになった。

## 51. ドキュメント追加: データリネージとマッピング定義書の作成

- **課題:** システムが複雑化する中で、どのAPIから取得したデータが、どのテーブルを経由して最終的なUI（帳票）のどの列にマッピングされるのかという因果関係（データリネージ）が不明格であり、上流と下流の不一致リスクを精査することが困難だった。
- **解決策:** 全13テーブル、全カラムの源泉と用途を網羅した `docs/data_lineage_and_mapping.md` を新規作成し、MkDocsのサイドメニューから閲覧可能にした。
- **効果:** エンジニアがシステム全体のデータの流れを完全に把握できるようになり、スキーマ変更時やトラブルシューティング時におけるアーキテクチャの安全性が担保された。

## 52. ドキュメント修正: DEVELOPING.md の「ドキュメントの歩き方」の更新

- **課題:** 新しく作成した `docs/data_lineage_and_mapping.md` の存在や用途が、開発者のガイドラインである `DEVELOPING.md` に記載されておらず、認知されないリスクがあった。
- **解決策:** `DEVELOPING.md` の「5. ドキュメントの歩き方」の表に `data_lineage_and_mapping.md` の行を追加し、役割とユースケースを明記した。
- **効果:** 新規参画者や他の開発者が、データ構造を調査する際に迷わず適切なドキュメントへアクセスできるようになった。

## 53. ドキュメント修正: Runbook（運用手順書）と自動デプロイスクリプトの乖離解消

- **課題:** `scripts/bootstrap-deploy.sh` ではデプロイ時に8つのSQLファイルを実行しているが、手動リカバリ手順書である `docs/operations-runbook.md` には `003_reconciliation.sql`, `006_matrix_pivot.sql`, `008_update_bindings_history.sql` の3ファイルの実行手順が漏れており、障害復旧時に環境が不完全になる（Runbook Drift）リスクがあった。
- **解決策:** `operations-runbook.md` のSQL適用手順セクションに、自動スクリプトと完全に同等のコマンド/リストを追記し、同期させた。
- **効果:** 運用チームが手動で環境を再構築する際にも、不整合検知や帳票ビュー等の全機能が正しくデプロイされることが保証された。

## 54. 重大バグ修正: デプロイスクリプトの初期データ構築順序と冪等性の欠如

- **問題:** `bootstrap-deploy.sh` において以下の3つの致命的な欠陥があった。
  1. 初期データを生成する `007_seed_workbook_from_existing.sql` がIAMデータ収集前に実行されるため、初回構築時にデータが生成されない（Chicken and Egg問題）。
  1. デプロイを再実行した際、`007` が無条件に再実行され、帳票データが重複増殖する。
  1. 初回データ収集フローから、最も重要な「IAMポリシーの収集」ジョブのトリガーが漏れていた。
- **解決策:** `007` の実行タイミングを Step 7（データ収集後）に移動し、IAMポリシー収集のトリガーを明記した。また、BigQueryの行数をチェックし、テーブルが空（`COUNT == 0`）の場合のみSeedを実行する冪等性（Idempotency）制御を追加した。
- **効果:** 新規環境構築時に確実に初期データがセットアップされ、何度デプロイスクリプトを再実行してもデータが破壊・重複されない堅牢なデプロイフローが完成した。

## 55. 重大バグ修正: GAS一括承認時のサイレント無視バグとSQLクラッシュ対策の適用

- **問題:**
  - `Code.gs` において、管理者がヘッダー行を含めてスプレッドシートを複数行編集した場合、すべての処理が早期リターンでスキップされ、画面上は承認済なのに権限が付与されないサイレント障害が発生する罠があった。
  - プリンシパル同期の `MERGE` 文クラッシュバグ、およびインシデント検知バッチの直積重複バグが残存していた。
- **解決策:**
  - `Code.gs` の `onEdit` を修正し、ヘッダー行のみをループ内で `continue` する設計に変更して、複数行の一括承認が確実に処理されるようにした。
  - `repository.py` のSQLを `ANY_VALUE() + GROUP BY` および `DISTINCT` を用いた堅牢な構文に修正した。
- **効果:** 運用者がスプレッドシート上でどのような範囲選択をして一括承認を行っても、確実にIAM権限が付与されるようになった。また、日次バッチの安全性が完全に担保された。

## 56. アーキテクチャ改善: GAS一括承認時のUIフリーズおよびタイムアウトの根本解決（FlaskセキュアバルクAPI化）

- **課題:** スプレッドシート（GAS）からの複数行の一括承認において、直列処理によるタイムアウト（GASの6分制限超過）のリスクがあった。しかし単純な非同期化を行うと、実行APIとの間でBigQueryの状態不整合（レースコンディション）が発生し、またフロントエンド（GAS）から監査スナップショットを送らせる設計は改ざんリスク（コンプライアンス違反）を伴うという複雑な課題が存在した。
- **解決策:**
  1. **バックエンド（Flask）の拡張:** `main.py` に `POST /api/requests/bulk-status` エンドポイントを新設した。
  1. **セキュアな状態取得:** `repository.py` に `bulk_update_request_status_and_history_secure` を実装。フロントエンドからの入力はIDとステータスのみに限定し、SQLインジェクションを防ぐパラメータ化クエリ (`IN UNNEST(@req_ids)`) を用いて、バックエンド側で安全に最新のスナップショットを取得・履歴保存する設計にした。
  1. **フロントエンドの責務分離:** GAS側は「ステータス更新の単一バルク送信（1回の通信）」を行い、その同期処理が成功した後にのみ「承認済みリクエストの `/execute` 並列発射」を行うよう、安全な実行順序を完全に担保した。
- **効果:** 数十件のコピペ一括承認が数秒で完了する圧倒的なパフォーマンス（N+1問題の解消）と、バックエンド主導の堅牢なセキュリティ（SQLインジェクション防止・監査ログの信頼性担保・レースコンディションの排除）を完璧に両立した。

## 57. 重大バグ修正: バルク処理におけるBigQuery Concurrent DMLエラー（N+1）の解消

- **課題:** UIからのバルクステータス更新処理 (`bulk_update_request_status_and_history_secure`) において、SQLインジェクション対策を施した結果、1件ずつ `UPDATE` 文を発行するループ処理になってしまっていた。これにより、数十件の一括承認時にBigQueryの同時実行制限（Concurrent DML）に抵触し、ジョブがクラッシュ・タイムアウトする致命的なデグレードが発生していた。
- **解決策:** ループ処理内での都度クエリ発行を廃止し、`CASE WHEN request_id = @req_id_1 THEN @new_status_1 ...` のように動的にパラメータ化された一括 `UPDATE` 文を組み立てる実装に変更した。
- **効果:** SQLインジェクションに対する完全な防御（セキュア）を保ったまま、数十件のステータス更新をわずか1回のDML通信で完結させることが可能になり、DML競合エラーとタイムアウトの危険性が完全に排除された。
